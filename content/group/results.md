---
title: Results
description: IB Diploma results and placements across the Fountainhead group.
draft: true
order: 35
---

> **TODO:** This page is a **stub recording the constraints only.** The
> placements and results build (WA-43, WA-44, WA-45) is substantial and was
> not scoped into the IA restructure — it needs its own pass.
>
> Inheritable, **not** `group-only`: WA-46 locks this as one pooled
> group-level page that every campus inherits. Marketing asked for
> campus-specific results pages; that was declined and WA-46 held.
>
> Binding conditions carried from the register:
> - **Campus attribution on every cohort** (WA-46). FWGS is a Wockhardt JV
>   and FALH/FASV are Avadh JVs — a parent browsing FWGS must not infer that
>   FSK's placements are FWGS's. The page states its group scope plainly.
> - **Two layers, never merged** (WA-45). The aggregate data layer carries no
>   names, photos or coordinates; the named celebration layer covers 2025+
>   cohorts only. The two must never combine on a page in a way that resolves
>   a size-1 bubble to a named student.
> - **No island** (WA-44). The map is static SVG generated at build time,
>   bubble radius = √count, `<title>` tooltips. Filters are pre-rendered
>   routes, not client state. Charts are hand-authored SVG — no chart
>   library. Looker, Google Maps JS, Leaflet, MapLibre and ECharts were all
>   considered and rejected.
> - **De-identified versioned dataset** (WA-43), rendered at build time and
>   PR-reviewed.
>
> Only FSK has DP cohorts today (2016–2026).
